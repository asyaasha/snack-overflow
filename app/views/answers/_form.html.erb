<%= form_for ([@question, @question.answers.build]) do |f| %>

  <p>
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </p>

  <p>
      <%= button_tag( :class => 'btn btn-default red darken-2') do %>
  Answer
<% end %>
  </p>

<% end %>
